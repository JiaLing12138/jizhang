# GitHub Pages 发布步骤（照做即可）

## 第一步：建仓库（2 分钟）

1. 手机或电脑浏览器打开 https://github.com/new 并登录
2. Repository name 填：`jizhang`（其他名字也行，但下面网址要跟着改）
3. 选 **Private**（私有，更安全）
4. 其他默认，点绿色 **Create repository**（先别建 README）

## 第二步：上传文件（1 分钟）

1. 在新仓库页面点 **uploading an existing file**（上传现有文件）
2. 把本文件夹里的 **index.html** 和 **personal.html** 两个文件拖进去
3. 点 **Commit changes**（提交）

## 第三步：开启 Pages（1 分钟，等 1-2 分钟生效）

1. 仓库页点 **Settings**（设置）→ 左侧 **Pages**
2. **Build and deployment** 里 Source 选 **Deploy from a branch**
3. Branch 选 **main**，文件夹选 **/ (root)**，点 **Save**
4. 等 1-2 分钟后刷新，页面顶部会出现网址：
   `https://<你的GitHub用户名>.github.io/jizhang/`

## 第四步：验证 + 迁移数据

1. 手机打开 `https://<用户名>.github.io/jizhang/personal.html`，能正常打开即可
2. **迁移旧账**（重要）：
   - 手机浏览器先打开旧网址（WorkBuddy 那个）
   - 点右上「备份」→ 把 JSON 文件存到「文件」App
   - 再打开新网址 → 点「导入」→ 选刚才的备份文件
   - 家庭台账 `index.html` 同样操作（它自己也有导出/导入按钮）
3. 把新网址发给 Codex，由 Codex 重新签名快捷指令（改地址 + 重签，1 分钟）

## 以后每次改页面怎么更新

1. 让 Codex 改好文件
2. 把 `github-upload/` 里的 index.html / personal.html 重新上传到仓库
   （仓库页 Code → 点文件 → 铅笔图标编辑 → 粘贴新内容 → Commit；或整批上传覆盖）
3. 网址不变，快捷指令不用动

> 注意：**网址域名定下来后别再换**，记账数据存在浏览器里、跟着域名走。
